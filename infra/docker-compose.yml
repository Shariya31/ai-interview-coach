# docker-compose placeholder
# Will define services:
# - auth-service
# - question-service
# - transcription-service
# - evaluation-service
# - analytics-service
# - frontend (nginx or shell)
version: "3.9"
services:
  mfe-shell:
    build: ../apps/mfe-shell
    ports:
      - "8080:80"
